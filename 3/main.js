window.onload = () => {
    class Rect {
        constructor(w, h) {
            this.width = w;
            this.height = h;
            this.x = 0;
            this.y = 0;
        }
        isInside(x,y) {
            
        }
    }

    var blocks = new Array();

    var btn = document.getElementById('spawn-element');
    var cnv = document.getElementById('canvas');
    var ctx = cnv.getContext('2d');

    function spawnBlock(x, y, w, h) {}

    function draw() {}
};
